#include<bits/stdc++.h>
using namespace std;

#define For(i,a,b) for(int i = a; i <= b; i++)

#ifdef LOCAL
#include "debug.h"
#else
#define deb(...) (void)0
#endif

int main() {
    int n; cin >> n; cout << n;
}